{
  "name": "tornado-atlas-verification",
  "private": true,
  "scripts": {
    "test:browser": "node --test --test-concurrency=1 tests/browser/*.test.mjs"
  },
  "devDependencies": {
    "playwright": "1.63.0"
  }
}
